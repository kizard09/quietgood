<%= form_for(@hero) do |f| %>
  <% if @hero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hero.errors.count, "error") %> prohibited this hero from being saved:</h2>

      <ul>
      <% @hero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <tr>
    <td class="field">
    <b><%= f.label :name, 'name' %></b><br>
    <%= f.text_field :name %>
  </td>
  </tr>
  <tr>
  <td class="field">
    <b><%= f.label :image, 'photo' %></b><br>
    <%= f.file_field :image %>
  </td>
  </tr>
  <tr>
  <td class="field">
    <b><%= f.label :description, 'bio' %></b><br>
    <%= f.text_area :description%>
  </td>
</tr>
<tr>
  <td class="actions">
    <%= f.submit %>
  </td>
</tr>

<% end %>
