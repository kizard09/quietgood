<%= form_for @read, :html => {:multipart => true} do |f| %>
  <% if @read.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@read.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @read.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <tr>
    <td class="field">
      <b><%= f.label :name, 'name' %></b><br>
      <%= f.text_field :name %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <b><%= f.label :book, 'photo' %></b><br>
      <%= f.file_field :book %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <b><%= f.label :synopsis, 'synopsis' %></b><br>
      <%= f.text_area :synopsis%>
    </td>
  </tr>
  <tr>
    <td class="actions">
      <%= f.submit %>
    </td>
  </tr>

<% end %>

